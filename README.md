# Molecular Prediction Model

A machine learning project for predicting molecular properties from SMILES strings.

## Current Status

We are setting up the backend foundation.

Completed so far:

- Created GitHub repository
- Opened project in GitHub Codespaces
- Created `develop` branch
- Created backend/frontend project structure
- Set up Python virtual environment inside `backend/`
- Installed backend dependencies
- Verified RDKit is working

## Project Structure

```text
Molecular-Prediction/
├── backend/
│   ├── app/              # FastAPI backend code later
│   ├── data/             # Molecular datasets
│   ├── models/           # Saved trained models
│   ├── notebooks/        # Experiments and analysis
│   ├── src/              # ML training/prediction code
│   ├── requirements.txt  # Backend Python dependencies
│   └── venv/             # Local virtual environment, not pushed
│
├── frontend/             # Frontend app later
└── README.md
